{
  "name": "alertmanager-webhook",
  "version": "1.0.0",
  "description": "Webhook to send Alertmanager notifications via Microsoft Graph API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@azure/identity": "^4.0.0",
    "@microsoft/microsoft-graph-client": "^3.0.7",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
